'use client';

import React from 'react';

// Hidden — content moved to the HamburgerMenu drawer.
// See DECISIONS.md for whether to restore or remove this component.
const Controls: React.FC<{ style?: React.CSSProperties }> = () => null;

export default Controls;
